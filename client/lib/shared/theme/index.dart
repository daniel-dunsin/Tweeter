import 'package:flutter/material.dart';

enum AppColorMode { light, dark }

class CustomTheme {
  static EdgeInsets blankScreenPadding = const EdgeInsets.all(40.0);
  static EdgeInsets majorScreenPadding =
      const EdgeInsets.fromLTRB(30, 20, 30, 30);
}
