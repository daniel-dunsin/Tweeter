import 'package:flutter/material.dart';

class TweetVideoPlayer extends StatelessWidget {
  const TweetVideoPlayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
